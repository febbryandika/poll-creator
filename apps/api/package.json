{
  "name": "@poll-creator/api",
  "version": "0.1.0",
  "private": true,
  "description": "Hono (Bun) public real-time API — vote + SSE live results over Redis pub/sub.",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.9.0",
    "@poll-creator/db": "workspace:*",
    "hono": "^4.12.0",
    "ioredis": "^5.11.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "catalog:",
    "@types/bun": "^1.3.0",
    "eslint": "catalog:",
    "eslint-config-prettier": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:",
    "vitest": "catalog:"
  }
}
